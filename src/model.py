import pandas as pd
from sklearn.ensemble import RandomForestRegressor
from sklearn.model_selection import train_test_split
from sklearn.metrics import mean_squared_error, r2_score

data = float('inf') # put whatever data i am going to have.
# in form: year, temp, disasters, ill cook up a formula for safeness so we
# can also have that so it can predict for different stuff

